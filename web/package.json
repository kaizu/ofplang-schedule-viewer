{
  "name": "ofplang-schedule-viewer",
  "private": true,
  "type": "module",
  "scripts": {
    "datasets": "node scripts/collect-datasets.mjs",
    "predev": "npm run datasets",
    "dev": "vite",
    "prebuild": "npm run datasets",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "pretest:e2e": "npm run datasets",
    "test:e2e": "playwright test",
    "preshots": "npm run datasets",
    "shots": "playwright test tests/e2e/shots.spec.ts"
  },
  "dependencies": {
    "yaml": "2.9.0"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "@types/node": "26.4.0",
    "typescript": "7.0.2",
    "vite": "8.2.2",
    "vitest": "4.1.11"
  }
}
